{
	"geode": "5.10.0",
	"gd": {
		"win": "2.2081",
		"android": "2.2081",
		"ios": "2.2081"
	},
	"id": "toastexgd.toastyreplay-lite",
	"name": "ToastyReplay-Lite",
	"version": "v1.0.12",
	"developers": [
		"Toastexgd",
		"Slideglide",
		"Anticroom",
		"Human0443",
		"Lyn",
		"DominoKiddoo"
	],
	"description": "A lightweight version of ToastyReplay with a native Geometry Dash interface.",
	"early-load": true,
	"tags": [
		"gameplay",
		"utility",
		"cheat",
		"offline"
	],
	"links": {
		"source": "https://github.com/ToastexGD/ToastyReplay-Lite"
	},
	"dependencies": {
		"geode.node-ids": ">=v1.23.3",
		"fryy_55.amber": {
			"version": ">=2.0.0",
			"required": true
		}
    },

	"incompatibilities": {
		"eclipse.eclipse-menu": {
			"version": ">=v0.0.0",
			"breaking": false
		},
		"toastexgd.toastyreplay": {
			"version": ">=v0.0.0",
			"breaking": true
		},
		"toastexgd.toastyreplay-pro": {
			"version": ">=v0.0.0",
			"breaking": true
		}
	},
	"settings": {
		"disable-input-precision": {
			"type": "bool",
			"name": "Disable CBF/CBS While Recording",
			"description": "Turn off Click Between Frames and Click Between Steps while recording or playing a replay, then restore them afterward.",
			"default": true
		},
		"watermark": {
			"type": "bool",
			"name": "Watermark",
			"description": "Show the ToastyReplay Lite logo and version in the corner while the menu is open or a macro is recording or playing.",
			"default": true
		},
		"frame-fixes": {
			"type": "bool",
			"name": "Frame Fixes",
			"description": "Record player position fixes after each physics tick. Windows replays vanilla inputs exactly without them, so this is off by default. Turn it on to play a macro from a start position it was not recorded from. Always on for Android and iOS.",
			"default": false,
			"platforms": ["win"]
		},
		"ignore-level-checks": {
			"type": "bool",
			"name": "Ignore Level Checks",
			"description": "Let any macro play in any level, even one it was not recorded on. Skips the level ID, level version, level data and start position checks. Copies of the same level play fine, genuinely different levels will desync and die.",
			"default": false
		},
		"show-floating-button": {
			"type": "bool",
			"default": false,
			"name": "Show Floating Button",
			"description": "Shows a floating button you can drag around to quickly open the menu. Designed for mobile users"
		}
	},
	"resources": {
		"files": [
			"resources/*.png"
		]
	}
}
